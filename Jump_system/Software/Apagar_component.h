#ifndef APAGAR_COMPONENT_H_INCLUDED
#define APAGAR_COMPONENT_H_INCLUDED

unsigned char eraseStateMachine(struct Menu* subMenu);

struct samples;
struct results;
struct Menu;
#endif // APAGAR_COMPONENT_H_INCLUDED
