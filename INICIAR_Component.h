#ifndef INICIAR_COMPONENT_H_INCLUDED
#define INICIAR_COMPONENT_H_INCLUDED

unsigned char initStateMachine(unsigned char* beginState);
unsigned char setCommunication();
unsigned char setParameters();
unsigned char startReading();


#endif // INICIAR_COMPONENT_H_INCLUDED
